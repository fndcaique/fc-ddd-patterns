name: Linting

on: pull_request

jobs:
  biome:
    name: Biome
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: v20.20.2

      - run: npm ci

      - run: npm run lint
